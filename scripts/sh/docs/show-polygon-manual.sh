#!/bin/sh

. scripts/sh/color.sh

echo ""
echo "Tools for problem setting using polygon: https://polygon.codeforces.com."
echo ""
echo "${Bold}Usage: polygon Command [Args..]${Normal}"
echo ""
echo "${Bold}Commands:${Normal}"
echo "  ${Bold}v${Normal}, ${Bold}validator [args]${Normal}      compile and run the validator, run with the -h or"
echo "                           --help flag for help"
echo "  ${Bold}g${Normal}, ${Bold}generator [args]${Normal}      compile and run a generator, run with the -h or"
echo "                           --help flag for help"
echo "  ${Bold}-h${Normal}, ${Bold}--help${Normal}               show this manual, then exit"
