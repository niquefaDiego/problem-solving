#include <bits/stdc++.h>
#ifdef LOCAL
#include "debug.h"
#else
#define debug(...) 42
#endif
using namespace std;

int main() {
  cin.tie(0);
  ios::sync_with_stdio(0);

  int a, b;
  cin >> a >> b;
  cout << a+b << "\n";

  return 0;
}